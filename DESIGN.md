# DESIGN.md — 미니멀 디자인 시스템

정토회 청년지부 물품 대여 사이트의 UI/UX 디자인 가이드다.  
핵심 철학은 **"UI는 투명하게 물러나고, 물품과 필요한 동작만 남긴다"**는 미니멀리즘이다.  
불필요한 장식, 과도한 계층 분할, 복잡한 색상 규칙을 걷어내고, 누구나 3초 안에 직관적으로 물품을 찾고 대여할 수 있는 단순함을 지향한다.

---

## 1. 핵심 원칙 (Core Principles)

1. **단일 평면 (Single Surface)**
   - 캔버스 위에 카드를 겹겹이 쌓아 올리는 복잡한 층위(파치먼트 ↔ 화이트 등)를 배제한다.
   - 기본 캔버스(순백/순흑) 위에서 **여백(Whitespace)**과 **1px 미세 헤어라인(Border)**만으로 영역을 단정하게 구획한다.
2. **모노크롬 우선 & 단일 액센트 (Monochrome First)**
   - 화면은 차분한 무채색(Black, White, Gray)을 기본으로 구성한다.
   - 클릭 가능한 주요 액션(CTA)과 링크에만 **Action Blue (`#0066cc` / 다크 `#2997ff`)**를 단일 액센트로 사용한다.
3. **조형 언어의 단순 통일 (Unified Geometry)**
   - 8px, 18px, 9999px(알약) 등 제각각이던 라운드 값을 **표준 8px (컨트롤·버튼) / 12px (카드·모달)**로 정돈하여 시각적 통일감을 준다.
   - 어디는 알약형이고 어디는 사각형이던 혼란을 없애고 직관성을 높인다.
4. **마찰 없는 인터랙션 (Zero Friction UX)**
   - 모든 주요 흐름은 2~3단계 이내로 끝난다.
   - 물품 검색 → 상세 확인 → 수량 선택 후 [대여하기] 원클릭으로 대여 완료.
   - 불필요한 확인 팝업을 최소화하고 인라인 안내 및 실행 취소(Undo) 기반으로 전환한다.

---

## 2. 간소화된 색상 토큰 (Color Tokens)

과도하게 세분화되었던 5가지 상태 톤 토큰을 정리하고, 필수적인 흑백 기반 팔레트와 3가지 핵심 상태로 압축한다.

| 토큰 | 라이트 모드 | 다크 모드 | 용도 및 가이드 |
|---|---|---|---|
| `--color-bg` | `#ffffff` (순백) | `#0c0c0e` (딥블랙) | 기본 캔버스 배경 |
| `--color-surface` | `#f8f8fa` (아주 은은한 회색) | `#161618` | 인풋 배경, 마우스 호버 영역 |
| `--color-border` | `#e5e5ea` (1px 헤어라인) | `#262628` | 섹션 구분선, 카드 테두리 |
| `--color-text` | `#111113` (선명한 블랙) | `#f2f2f5` | 본문, 제목, 주요 텍스트 |
| `--color-muted` | `#71717a` (차분한 그레이) | `#8e8e93` | 보조 설명, 라벨, 메타 정보 |
| `--color-primary` | `#0066cc` (Action Blue) | `#2997ff` | 주 CTA 버튼, 활성 탭, 텍스트 링크 |
| `--color-primary-text` | `#ffffff` | `#ffffff` | Primary 버튼 위 텍스트 |
| `--color-danger` | `#e11d48` | `#fb7185` | 삭제, 취소, 거절 등 파괴적 액션 |

### 미니멀 상태 인디케이터 (Status Dots)
복잡하고 알록달록한 배경색 배지 대신, **무채색 칩 + 상태 점(Status Dot)**으로 정갈하게 표현한다.
- **대여 가능**: `●` 은은한 녹색 인디케이터 (`#10b981`) + "대여 가능" 텍스트
- **대여 중**: `○` 차분한 무채색 인디케이터 (`#71717a`) + "대여 중 (남은 수량)"
- **수리/점검**: `!` 은은한 주황색 인디케이터 (`#f59e0b`) + "수리 중"

---

## 3. 타이포그래피 & 간격 스케일

불필요하게 쪼개진 폰트 단계를 4단계로 단순화하고, 굵기는 **400(Regular)**과 **600(SemiBold)** 두 가지만 사용한다.

```text
대제목 (Page Title)    : 1.25rem  (20px) / 600 / 행간 1.2
섹션/카드 제목 (Heading): 1.0625rem (17px) / 600 / 행간 1.3
본문 (Body)           : 0.9375rem (15px) / 400 / 행간 1.5   --text-body
캡션/보조 (Caption)    : 0.8125rem (13px) / 400 / 행간 1.4   --text-caption
```

- **간격(Spacing)**: 4의 배수 (`4px`, `8px`, `16px`, `24px`, `32px`) 5가지만 사용하여 여백의 리듬을 단순화한다.
- **라운드(Radius)**:
  - `--radius-sm`: `6px` (태그, 작은 칩)
  - `--radius-md`: `8px` (버튼, 인풋 필드, 기본 컨트롤)
  - `--radius-lg`: `12px` (카드, 다이얼로그, 이미지 썸네일)

---

## 4. 컴포넌트 디자인 사양

### 1) 버튼 (Buttons) — 단 2종으로 단일화
- **Primary 버튼**: 
  - `--color-primary` 솔리드 배경 + 흰색 텍스트 + `8px` 라운드.
  - 높이 `42~44px`, 폰트 `15px / 600`.
  - 페이지당 가장 중요한 1개의 주 액션(대여 신청, 등록 완료)에만 사용.
- **Secondary 버튼 (Ghost / Outline)**:
  - 투명 배경 + `--color-border` 1px 테두리 + 본문 텍스트 색상.
  - 호버 시 은은한 `--color-surface` 배경 전환.
  - 취소, 닫기, 수정 등 보조 액션에 공통 적용.
- **파괴적 액션 (Danger)**:
  - 텍스트 링크 형태 또는 붉은색 텍스트의 고스트 버튼으로 절제되게 노출.

### 2) 검색창 & 입력 필드 (Inputs)
- 44px 일관된 터치 타깃 높이, `8px` 라운드.
- `--color-surface`의 아주 은은한 배경색과 1px 헤어라인.
- 포커스 시 테두리만 깔끔하게 `--color-primary`로 전환 (두꺼운 아웃라인 지양).
- 검색창은 상단에 간결한 돋보기 아이콘과 함께 단일 라인으로 배치.

### 3) 물품 카드 (Item Card)
- 카드 안을 복잡한 박스로 채우지 않고, **정갈한 사진 썸네일(1:1 또는 4:3 비율, 8px 라운드) + 물품명 + 상태 인디케이터**만 심플하게 배치.
- 마우스 호버 시 카드가 위로 튀어오르거나 진한 그림자가 생기지 않고, 테두리 색상만 미세하게 선명해지는 절제된 피드백.

### 4) 모달 & 다이얼로그 (Dialogs)
- 풀스크린 블러나 무거운 애니메이션 대신, 가벼운 딤(Dim) 배경과 부드러운 페이드인.
- 화면 중앙에 최대 너비 `480px` 내외로 간결하게 배치하며, 제목 - 본문 폼 - 하단 버튼(취소 / 확인)의 단순한 3단 구조 고수.

---

## 5. 단순화된 UX 인터랙션 흐름

1. **대여 프로세스 (Fast-Track Rental)**
   - [홈] 목록에서 물품 클릭 → [상세] 바로 수량 조절(`-`, `+`)과 메모 1줄 입력 → `[대여하기]` 버튼 클릭 → 대여 완료 토스트 후 마이페이지로 이동.
   - 복잡한 날짜 선택, 대여 목적 선택 등의 군더더기 질문 완전 배제.
2. **반납 프로세스 (One-Tap Return)**
   - 마이페이지 내 대여 항목 우측의 `[반납]` 버튼 1클릭 → 즉시 반납 완료 처리.
3. **관리자 모드 (Inline Management)**
   - 복잡한 별도 어드민 콘솔 분기 대신, 관리자 권한이 있는 사용자에게는 물품 상세 화면에서 즉시 `[수정]` 및 `[사진 관리]` 버튼이 노출되는 일체형 경험.

---

## 6. Do & Don't (미니멀 체크리스트)

- ✅ **Do**: 정보를 전달하는 글자와 사진이 UI보다 돋보이게 한다.
- ✅ **Do**: 보더와 배경색의 종류를 최소화하고 균일한 여백으로 정돈한다.
- ✅ **Do**: 모든 클릭 요소는 40px 이상의 여유 있는 터치 영역을 보장한다.
- ❌ **Don't**: 알록달록한 형광색 배지나 5가지 이상의 상태 컬러를 섞어 쓰지 않는다.
- ❌ **Don't**: 그림자(`box-shadow`)나 그라데이션 장식을 넣지 않는다. (평면과 헤어라인 유지)
- ❌ **Don't**: 어디는 둥근 알약형, 어디는 직각 사각형으로 라운드 값을 혼용하지 않는다. (8px/12px 통일)
- ❌ **Don't**: 단순 확인을 위해 클릭 단계를 늘리는 불필요한 모달 팝업을 남발하지 않는다.
