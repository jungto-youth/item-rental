node_modules/
dist/
.wrangler/
.dev.vars
.env
*.local

# pi 에이전트 작업 로그·태스크 출력 (로컬 스크래치)
.pi/

# 시트 반영 파이프라인의 중간 산출물.
# data/sheet-import/review-rentals.csv 에 2025 대여 이력 신청자 실명 27명이 들어 있어
# 공개 저장소(this repo)에는 올리지 않는다. build.py·review.py 로 언제든 재생성된다.
data/
