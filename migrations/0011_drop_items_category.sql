-- 카테고리(분류) 제거 — 사용자 결정.
--
-- 0010에서 시트 '분류' 13값으로 TEXT 컬럼을 되살렸으나 철회한다. 이유:
--   분류로 물품을 찾는 대표 사례가 대부분 검색어로 직접 표현된다('조끼', '수신기').
--   154건을 13 탭으로 쪼개면 오히려 원하는 물품이 어느 탭에 섞였는지 헤매게 된다
--   (예: 조끼가 역사기행/청년행사 양쪽에 걸쳐 있다). v2.4~2.5가 카테고리를 제거하고
--   키워드+의미 검색으로 탐색을 대체한 것과 같은 판단이며, 실물 데이터도 그 판단을
--   뒷받침한다. 위치(location)는 유지 — 물건 찾기는 위치 그 자체다.
ALTER TABLE items DROP COLUMN IF EXISTS category;

DROP INDEX IF EXISTS idx_items_category;
